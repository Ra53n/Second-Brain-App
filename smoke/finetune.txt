# Смоук раздела «Тюнинг» (задачи 81, 82). Шаги: open <раздел> | click <подпись> | check <подпись>
# Кнопку старта («Запустить тюн») не жмём — реальный прогон час и 11 ГБ (DESTRUCTIVE),
# только check (ассерт присутствия, не нажатие). «Проверить датасет» жать можно:
# секунды, чистый python, ничего не портит.
#
# Клик по СТРОКЕ списка датасетов через Accessibility ненадёжен на этом экране
# (тот же класс проблем, что и клик по сайдбару, см. /smoke-ui) — сценарий этого
# не требует: «Baseline»/«Критерии» сами подставляют первый датасет, если ничего
# не выбрано, «Датасеты» после них — тот же датасет, выбранный так же.
#
# Порядок нарочно идемпотентен (ревью задачи 82, Б2): detail роутится строго по
# активному сегменту (Б1), не по тому, что осталось выбранным с прошлого прогона
# без рестарта, поэтому «click <сегмент>» перед каждым «check» — не полагаемся на
# состояние, унаследованное от предыдущего запуска этого же сценария. Явный повторный
# заход на «Прогоны» после «Baseline»/«Критерии» — это и есть проверка Б1: раньше
# автовыбор датасета для Baseline подменял detail «Прогонов» на экран датасета.
open Тюнинг
check Датасеты
check Прогоны
check Baseline
check Критерии
click Прогоны
check Запустить тюн
click Baseline
check 10 из 12 примеров eval-сплита
check Ответ модели
check Эталон (текст автора)
click Критерии
check Критерии оценки
click Прогоны
check Запустить тюн
click Датасеты
check Проверить датасет
click Проверить датасет
check OK — датасет валиден
check уникальных текстов assistant
