# Смоук раздела «Тюнинг» (задачи 81, 82, 83). Шаги: open <раздел> | click <подпись> | check <подпись>
# Навигация задачи 83: слева список датасетов, справа вкладки датасета
# (FineTuneTabBar): Обзор | Примеры | Baseline | Критерии | Прогоны. Вкладки —
# plain-кнопки, их подпись доходит до AX только через accessibilityValue
# (активная — «<имя> — выбрано»), клик и check работают по подстроке value.
#
# Кнопки с побочными эффектами не жмём (DESTRUCTIVE в ui.sh): «Запустить тюн»
# (час и 11 ГБ), «Снять baseline» (минуты генерации mlx), «Сгенерировать
# критерии» (токены облачного LLM), «Импортировать» — только check присутствия.
# «Проверить датасет» жать можно: секунды, чистый python, ничего не портит.
#
# Клик по СТРОКЕ списка датасетов через Accessibility ненадёжен — сценарий его
# не требует: detail сам подставляет первый датасет (selectFirstDatasetIfNeeded).
# Порядок идемпотентен: перед каждым check — явный click вкладки, состояние
# прошлого прогона не наследуется.
open Тюнинг
check Добавить датасет
check Обзор
check Примеры
check Baseline
check Критерии
check Прогоны
click Обзор
check Обзор — выбрано
check Валидация
check Обучение
check Сравнение
click Примеры
check Примеры — выбрано
check Проверить датасет
click Проверить датасет
check OK — датасет валиден
click Baseline
check Baseline — выбрано
check Снять baseline
check Ответ модели
check Эталон (текст автора)
click Критерии
check Критерии — выбрано
check Сгенерировать критерии
check Критерии оценки
click Прогоны
check Прогоны — выбрано
check Запустить тюн
check Взять лучший чекпоинт
# Гвард installBest (задача 92, isCurrentRun) отключает эту кнопку без прогона/
# выбора и меняет .help на пояснение про перезаписанный run.json — оба факта
# check не видит (нет чтения AXEnabled/AXHelp в ax.applescript), проверка
# состояния — руками. Пресеты «3B»/«7B» у поля модели — Button внутри Form/
# Section: по опыту этого экрана AX не отдаёт им подпись вовсе (см. комментарий
# в FineTuneRunViews.swift), смоук их не покрывает — клик руками.
click Обзор
check Обзор — выбрано

# Вкладка «Чат» (задачи 85–91): мини-чат тюна с пайплайном уверенности и
# каскадной эскалацией. «Отправить» и «Батч: …» — DESTRUCTIVE (реальный
# инференс mlx), только check. Тумблер «Эскалация» жать можно (side effect —
# только запись пикера в finetune-chat.json, без сети): включаем, проверяем
# пикер цели, выключаем обратно — состояние на выходе как на входе.
#
# Пикер базы чата (задача 92): дефолт — 7B (легаси), проверяем только
# присутствие/значение — клик переключает базу и рестартует mlx-сервер при
# следующей отправке, тестировать переключение здесь не по чем (нет прогона 3B).
#
# Цель эскалации на локальную тюненую модель (задача 93): пункт «Тюн <база>
# (локально)» появляется в пикере, только когда у датасета meetings есть хотя
# бы один finished-прогон (viewModel.availableTunedModels) — в рабочей копии
# для смоука таких нет (finetune-runs.json: runs пуст, обучение — час и 11 ГБ,
# не по чем гонять здесь), поэтому секция «Локально (mlx)» отсутствует и check
# по её пункту не заводится: пикер и без него обязан остаться рабочим (check
# «Не выбрано» ниже). Присутствие пункта и выбор — TuningChatViewModelTests
# (`availableTunedModels`) и ручная проверка после реального завершённого тюна.
click Чат
check Чат — выбрано
check Qwen2.5-7B-Instruct-4bit
check Статистика — Базовая
check пока нет ответов
check Очистить чат
check Проверки
check Повторы ×3
check Скоринг
check Self-check
check Эскалация — выключено
click Эскалация
check Эскалация — включено
check Не выбрано
click Эскалация
check Эскалация — выключено
check Отправить
