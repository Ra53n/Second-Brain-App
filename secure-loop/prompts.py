# prompts.py — чистые шаблоны промптов Дня 14: генерация и security review.
# Без I/O: loop.py подставляет задачу, контекст и код.

# Инструкция кодогенерации уходит в user-промпт целиком: системный промпт
# gateway глобальный и для харнеса не перенастраивается на каждый вызов.
GENERATION_HEADER = """\
Ты — Swift-инженер. Пиши код для macOS 14+ (Swift 5.9, только Foundation).
Правила ответа:
- верни РОВНО ОДИН блок кода в ```swift … ``` и ничего вокруг;
- код — один самодостаточный файл, компилируется в составе SPM-библиотеки Sandbox;
- без @main, без main.swift, без сторонних зависимостей.

Задача:
{task}

Файлы проекта (контекст):
{context}
"""

BUILD_FIX_FOOTER = """\

Предыдущая версия файла {filename} не собралась. Ошибки компилятора:
{errors}

Исправь и верни файл целиком одним блоком ```swift … ```.
Предыдущая версия:
```swift
{previous}
```
"""

SECURITY_FIX_FOOTER = """\

Security review отклонил предыдущую версию файла {filename}. Исправь замечания:
{issues}

Верни файл целиком одним блоком ```swift … ```.
Предыдущая версия:
```swift
{previous}
```
"""

# Security-промпт под стек Swift/macOS (пункты из домашки: Keychain vs UserDefaults,
# hardcoded secrets, PII в логах, http/ATS, валидация ввода).
SECURITY_REVIEW = """\
Ты — security-ревьюер iOS/macOS кода. Проверь Swift-код ниже по чек-листу:
1. Секреты (токены, ключи, пароли) в UserDefaults, plist или файлах вместо Keychain.
2. Hardcoded secrets: зашитые в код ключи, токены, пароли.
3. Токены, Authorization-заголовки, PII (email, телефоны, имена) в логах
   (print, NSLog, os_log, Logger).
4. http:// вместо https://, отключение ATS (NSAllowsArbitraryLoads), доверие
   любым сертификатам.
5. Отсутствие валидации входных данных (URL, JSON, пользовательский ввод)
   перед использованием.
6. Прочее: SQL-конкатенация, предсказуемые временные файлы, force unwrap на
   данных из сети.

Шкала severity:
- critical — прямая утечка секретов или данных пользователя;
- high — серьёзный риск (секрет вне Keychain, PII/токены в логах, http);
- medium — заметная слабость (нет валидации, force unwrap на сетевых данных);
- low — гигиена и стиль безопасности.

Ответ — ТОЛЬКО валидный JSON без markdown и пояснений:
{{"findings":[{{"severity":"critical|high|medium|low","line":<номер>,"issue":"<кратко, по-русски>"}}]}}
Если проблем нет: {{"findings":[]}}

Код файла {filename} (строки пронумерованы):
{code}
"""

JSON_RETRY = """\
Твой предыдущий ответ не распарсился как JSON. Повтори вердикт по тому же коду.
Верни ТОЛЬКО валидный JSON вида
{"findings":[{"severity":"…","line":1,"issue":"…"}]} — без markdown, без текста вокруг.
"""
