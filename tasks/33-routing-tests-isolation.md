# 33 — Изоляция тестов роутинга от реального routing.json

## Цель
Тесты не смеют трогать данные пользователя: прогон `swift test` удалял и перезаписывал настоящий `Application Support/SecondBrain/routing.json` — пользователь молча терял назначения моделей (обнаружено: пропал выбор эмбеддинга Ollama · bge-m3, чип «База» требовал переиндексацию).

## Зависимости
07/17 (роутинг), обнаружено при задаче 31.

## Контекст (диагноз)
- `FunctionRoutingStoreTests` писали/удаляли по `FunctionRoutingStore.fileURL` — реальный путь (testLoadMissingFileGivesEmptyConfig удалял файл напрямую).
- `FunctionRouter.assign()` сохранял в реальный путь — `FunctionRouterTests` перезаписывали конфиг мусорными назначениями.
- Это нарушение конвенции проекта (тесты — только temp; данные пользователя неприкосновенны) из задачи 17, жившее с тех пор незамеченным.

## Объём работ
- [ ] `FunctionRoutingStore.load(from:)/save(_:to:)` параметризованы URL (дефолт — прежний путь).
- [ ] `FunctionRouter` + `storeURL:` (тесты подставляют temp; приложение — дефолт).
- [ ] FunctionRoutingStoreTests на temp-файлах (setUp/tearDown), FunctionRouterTests.makeRouter с temp storeURL.
- [ ] Проверка: после `swift test` реальный routing.json не создаётся/не изменяется.
- [ ] Восстановить пользователю затёртое назначение: «Эмбеддинги» → Ollama · bge-m3.

## Вне объёма
- Аудит остальных сторов на ту же проблему (SettingsStore/ChatStore/MCPServerStore уже инжектируют fileURL в тестах — проверено grep'ом по прежним задачам).

## Критерии приёмки
- `swift test` зелёный и не оставляет/не удаляет реальный routing.json.

## Результат

Сделано (2026-07-18): все пункты объёма. После прогона 658 тестов реальный routing.json отсутствует (тесты его больше не создают и не трогают); назначение эмбеддингов bge-m3 восстановлено записью конфига при перезапуске приложения. Урок: любой новый стор обязан принимать URL и в тестах получать temp — добавлен регресс-тест `testDefaultFileURLPointsToAppSupport` + комментарии-предупреждения в тестах и сторе.