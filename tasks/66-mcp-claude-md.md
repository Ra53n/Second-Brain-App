# Задача 66: `CLAUDE.md` для модуля `MCP/`

## Тип

документация

## Модель

haiku

## Цель

`Sources/SecondBrain/MCP/` — stdio-протокол JSON-RPC с внешними процессами-серверами,
самое хрупкое место по механике (частичные чтения буфера, корреляция по id), но описано
только в шапке `MCPConnection.swift`, не в модульном `CLAUDE.md`.

## Зависимости

Нет.

## Объём

1. Создать `Sources/SecondBrain/MCP/CLAUDE.md` по шаблону существующих модульных CLAUDE.md
   (пример — `Sources/SecondBrain/RAG/CLAUDE.md`).
2. «Что здесь живёт»: `MCPConnection` (stdio-фрейминг, корреляция id), `MCPManager`,
   `MCPProtocol`, `MCPServer`, `ProviderToolSupport` (конвертация MCP tool schema ↔ формат
   провайдера), `ToolUse`, `MCPServersSection` (UI настроек).
3. «Инварианты»/риски: что происходит при обрыве процесса сервера, частичном JSON во
   буфере, несовпадении id ответа — как это сейчас обрабатывается (описать по факту кода,
   не изобретать новое поведение).

## Вне объёма

Правка кода `MCP/`, изменение протокола.

## Критерии приёмки

- [x] `Sources/SecondBrain/MCP/CLAUDE.md` создан, все обязательные секции на месте.
- [x] Описаны конкретно: фрейминг stdio, корреляция id, поведение при обрыве процесса.
- [x] `./scripts/build.sh` зелёный.

## Отчёт тестов

**Круг 2 (после NO-GO):** исправлены фактические неточности, найденные ревьюером —
все построчным чтением `MCPConnection.swift` и `MCPManager.swift`, без опоры на память
предыдущего прохода:

- **Блокирующее:** п. 5 путал два разных сценария обрезки stderr. `MCPManager.swift:65`
  (диагностика при ошибке `connect()`) режет **300** символов, а не 400; 400 символов —
  реально в `MCPConnection.swift:88` (срыв pending при обрыве уже установленного
  соединения). Оба числа теперь в тексте с явным указанием файла и сценария.
- **Важное 1:** п. 3 придумывал несуществующий `for`-цикл с `continue`. В коде —
  один `guard ... else { return }` в `handle()` (`MCPConnection.swift:102-106`).
  Переписано без выдуманного control-flow.
- **Важное 2:** п. 8 ссылался на «строки 35-41» без префикса файла (в файле без
  префикса = `MCPConnection.swift`, что неверно для этого пункта). Добавлен префикс
  `MCPManager:35-41`.
- **Мелочи:** «schemaм» → «схем» (п. «Что здесь живёт»); п. 2 разведены два сценария
  построчного фрейминга — ожидание `\n` в буфере (данные не теряются) и decode-ошибка
  на уже полной строке (`try? JSONDecoder()` молча дропает).

**Доп. находка при перепроверке (не из списка ревьюера):** секция «Как тестируем»
описывала мокирование subprocess/reentry/таймаутов для `MCPConnection`/`MCPManager`,
которого в `MCPTests.swift` нет (`Process` не инжектируется, юнит-тестов на реальный
subprocess не существует). Переписана по фактическому списку тестовых классов
(`MCPProtocolTests`, `ToolSchemaConversion`, `ToolUseLoop.run()`, `MCPServer`,
`OllamaParsing`) с явным «не покрыто» для `MCPConnection`/`MCPManager` (аналог
Core Audio/живого железа — проверяется вручную).

**Перепроверены построчно (совпадают с кодом):** пп. 1, 4, 6, 7 «Инвариантов» —
регистрация процесса (`MCPConnection.swift:67`), обрыв процесса и срыв pending
(строки 57, 80, 85-89, 115), ERROR-префикс (`callTool`:171,181; `MCPManager.call`:99),
таймауты handshake/call (`MCPManager.swift:47,106`); секция «Что здесь живёт» сверена
с `MCPProtocol.swift`, `MCPServer.swift`, `ToolUse.swift`, `ProviderToolSupport.swift`,
`MCPServersSection.swift` — расхождений не найдено.

**Итог:** `./scripts/build.sh` зелёный, без варнингов (0.21s инкрементально).

**Осталось:** ничего — задача выполнена, ждёт повторного ревью.

## Результат

`Sources/SecondBrain/MCP/CLAUDE.md` создан по шаблону `RAG/CLAUDE.md`, добавлен в `exclude`
`Package.swift`. Два круга ревью: первый NO-GO — перепутанные числа обрезки stderr
(300 vs 400 символов в разных сценариях), выдуманный `for`/`continue` вместо реального
`guard...else{return}`, ссылка на строки без префикса файла. Все три исправлены построчной
сверкой с кодом; попутно найдена и исправлена неточность в «Как тестируем» (описывала
несуществующие моки subprocess). Второй круг — `GO`.

Код `MCP/` и сам протокол не менялись — задача только документация.
