# Задача 64: `CLAUDE.md` для модуля `Pipelines/`

## Тип

документация

## Модель

haiku

## Цель

`Sources/SecondBrain/Pipelines/` (движок автоматизаций: cron, PR-watch, code review) —
самый крупный модуль без своего `CLAUDE.md`. Следующему агенту, который трогает пайплайны,
не нужно каждый раз перечитывать `docs/ARCHITECTURE.md` целиком или гадать по коду.

## Зависимости

Нет.

## Объём

1. Создать `Sources/SecondBrain/Pipelines/CLAUDE.md` по шаблону существующих модульных
   CLAUDE.md (пример — `Sources/SecondBrain/RAG/CLAUDE.md`): «Что здесь живёт», «Инварианты»,
   «Как тестируем», «Частые ошибки прошлых задач» (если есть, иначе секцию не выдумывать —
   можно опустить или написать «пока нет»), «Куда не лезть».
2. Разделы «Что здесь живёт» покрывают минимум: `PipelineEngine`, `PipelineScheduler`,
   `PipelineStore`, `CronExpression`, `PRWatcher`, `PipelineModels`, `PipelineViews`,
   подпапку `CodeReview/` (GitHub-клиент, сборка ревью-инпута, раннер).
3. «Инварианты» — из инварианта №2 проекта (жизненный цикл фоновых процессов) в приложении
   к пайплайнам: как регистрируются/гасятся процессы, если пайплайн их запускает.

## Вне объёма

Правка самого кода `Pipelines/`, `docs/ARCHITECTURE.md` (если там уже есть описание —
сослаться на него, а не дублировать/переписывать).

## Критерии приёмки

- [x] `Sources/SecondBrain/Pipelines/CLAUDE.md` создан, все обязательные секции на месте.
- [x] Файл соответствует стилю остальных модульных CLAUDE.md (см. `RAG/CLAUDE.md`,
      `Chat/CLAUDE.md`) — комментарии по делу, без пересказа кода.
- [x] `./scripts/build.sh` зелёный (документация не должна ничего сломать по определению,
      но прогнать для чистоты, раз того требует правило верификации).

## Отчёт тестов

**Круг 1 — NO-GO от reviewer**, устранено:
- Блокирующее 1: `Pipelines/CLAUDE.md` не был в `exclude` таргета `SecondBrain`
  (`Package.swift`) → варнинг unhandled resource. Добавлена строка `"Pipelines/CLAUDE.md"`
  в список `exclude` рядом с остальными модульными CLAUDE.md.
- Блокирующее 2: раздел «Куда не лезть» ошибочно утверждал, что постинг комментариев
  в PR не реализован / вне модуля. На деле реализован (`GitHubClient.postComment`,
  `CodeReviewRunner.postStartComment`/`autoPostIfConfigured`, вызов автопоста в
  `PipelineEngine`) и работает через подтверждение пользователя либо явный
  `autoPostReviewComment`. Раздел переписан по факту (сверено с `docs/ARCHITECTURE.md`,
  раздел «Code review»); ссылка на несуществующее «правило бэклога №16 про GitHub API»
  убрана (пункт 16 бэклога — про write-инструменты чата write_file/edit_file, к GitHub
  отношения не имеет). Тот же факт продублирован пунктом 9 в «Инвариантах» — реальное
  правило (постинг только через подтверждение/явный автопост), не пересказ кода.
- Мелочь: «rate-limit floor 5 мин без токена» → «жёсткий пол `PipelineTrigger
  .minPollIntervalMin` = 5 мин независимо от наличия токена» (сверено с
  `PipelineModels.swift:33,49-52`).
- Мелочь: инвариант «стоп-условие планировщика» был пересказом `PipelineScheduling
  .duePipelines`, не жёстким правилом — убран из «Инвариантов» (нумерация пересчитана,
  8→7, 9→8), добавлен настоящий инвариант про постинг в PR (пункт 9).

**Проверено:** `./scripts/build.sh` — зелёный, `Build complete! (3.46s)`, варнинга про
unhandled resource больше нет.

**Структура документации (без изменений с круга 1):** «Что здесь живёт» — все ключевые
типы модуля; «Как тестируем» — чистые функции без I/O, моки провайдеров; «Частые ошибки» —
4 реальных проблемы из истории проекта.

## Результат

`Sources/SecondBrain/Pipelines/CLAUDE.md` создан по шаблону `RAG/CLAUDE.md`/`Chat/CLAUDE.md`,
добавлен в `exclude` `Package.swift`. Два круга ревью: первый NO-GO — файл не был исключён из
сборки (варнинг unhandled resource) и раздел «Куда не лезть» неверно описывал постинг
комментариев в PR как нереализованный. Оба устранены и подтверждены сверкой с кодом
(`GitHubClient.postComment`, `CodeReviewRunner`, `PipelineEngine`). Второй круг — `GO`.

Код `Pipelines/` и `docs/ARCHITECTURE.md` не менялись — задача только документация.
