# Backlog проекта

Единый бэклог: сюда записываются **все** будущие задачи проекта. Правила:

- Новая идея/задача → запись здесь (название, абзац описания, зависимости, дата и происхождение).
- Когда задача берётся в работу, она «вырастает» в нумерованный файл `tasks/NN-slug.md` в стандартном формате (Цель/Зависимости/Объём работ/Вне объёма/Критерии приёмки) и добавляется строкой в [00-INDEX.md](00-INDEX.md). Здесь пункт помечается `→ NN`.
- Приоритет финально выбирает пользователь; порядок ниже — рекомендация.
- История: до 2026-07-16 бэклог жил в [19-polish-backlog.md](19-polish-backlog.md).

## Приоритетный список

1. **Сравнение провайдеров транскрипции** (после 08, 10, 11): одна запись → транскрипция всеми настроенными провайдерами параллельно → экран сравнения бок-о-бок + стоимость/время. Пользователь прямо просил возможность определить лучшего для своих задач. Эталон UI: ComparisonView в MA.
2. **Диаризация спикеров** (после 08): Deepgram/AssemblyAI отдают спикеров — размечать транскрипт «Спикер 1/2», давать переименовывать в заметке.
3. **Глобальный хоткей записи** (после 06): начать/остановить запись из любого приложения (Carbon RegisterEventHotKey или MASShortcut-подобное), пункт в menu bar со статусом записи.
4. **Переименование заметки с обновлением ссылок** (после 04): LinkIndex это уже умеет по данным — нужна операция и UI.
5. **Graph view** (после 04): визуализация графа ссылок (Canvas/SpriteKit, force-directed).
6. **Теги** (после 04): `#тег` и frontmatter tags — парсинг, панель тегов, фильтр.
7. **Шаблоны заметок** (после 03): папка Templates, подстановки {{date}}, {{title}}; шаблон встречи для 11.
8. ~~**FSM-пайплайн ответов в чате**~~ → задача 35 (2026-07-18). Отложенные под-фичи см. пп. 23–26.
9. **Автозапись по календарю** (после 11): интеграция с Calendar.app (EventKit) — напоминание/автостарт записи в момент встречи, название встречи из события календаря (закрывает сценарий «название задано заранее» автоматически).
10. ~~**Подтверждение MCP-вызовов**~~ → задача 39 (2026-07-18): MCP-вызовы проходят слой разрешений (write-уровень, approve в режиме «Спрашивать»).
11. **Автообновления** (после 18): Sparkle 2.
12. **Экспорт/печать заметки** (после 03): PDF/HTML.
13. **Индикатор стоимости** (после 08): счётчик потраченного по провайдерам за месяц (метрики уже собираются — MessageMetrics).

## Отложено из задач 20–23 (2026-07-16)

14. **Gemini sendWithTools** (после 15): tool-use цикл для Gemini-провайдера — сейчас инструменты работают только с OpenAI-совместимыми провайдерами и Ollama (см. заголовок ToolUse.swift).
15. **Ветвление истории чата** (после 12): дерево сообщений (MsgNode из MA) вместо линейной истории; редактирование вопроса с ответвлением.
16. ~~**Подтверждение write-инструментов проекта**~~ → задача 39 (2026-07-18): write_file/edit_file/delete_file/run_command со слоем разрешений (режимы «Спрашивать»/«Авто»/«Авто-опасный», approve один раз или на сессию).
17. **Autocomplete-меню слэш-команд** (после 22): полноценный выпадающий список команд с навигацией стрелками и Tab-подстановкой (сейчас — текстовая подсказка над полем ввода).
18. **Настраиваемый бюджет контекста /help** (после 22): лимит символов [PROJECT_DOCS] в настройках (сейчас константа 32 000).
19. ~~**Индексация docs выбранного репо в RAG**~~ → задача 25 (2026-07-17).

## Добавлено по итогам smoke-теста (2026-07-17)

20. **Автозапуск Ollama при выборе локальной модели в чате** (после 09): выбор Ollama-модели в пикере чата мог бы предлагать запустить рантайм, а не просто показывать «не запущен».
21. ~~**UI статуса индекса доков проекта**~~ → задача 47 (2026-07-22): статус (файлы/чанки/модель/дата) и кнопка «Перестроить индекс» в настройках «Инструменты».
22. ~~**Smoke-чек-лист перед завершением UI-задач**~~ → задача 46 (2026-07-22): смоук всех кнопок затронутого экрана через Accessibility — обязательный шаг `/task-verify` и отдельный скилл `/smoke-ui` с проверенными osascript-рецептами.

## Отложено из задачи 35 (2026-07-18)

23. **Swarm-волны в FSM-чате** (после 35): параллельные подагенты внутри execution — планировщик размечает зависимости шагов (`ЗАВИСИМОСТИ:`), волны через topo-sort, TaskGroup. Эталон: runWave/computeWaves в MA.
24. **ASK_USER в FSM-прогоне** (после 35): уточняющий вопрос посреди прогона — статус awaitingInput, маркер ASK_USER, ответ пользователя продолжает тот же прогон. Эталон: PendingQuestion/answerClarification в MA.
25. **Router-диспетчер интеръекций** (после 35): сообщение пользователя во время активного прогона классифицируется LLM-роутером (REDO_CURRENT/BACK/REPLAN/RESTART/GOTO/REFUSE), код валидирует по таблице FSM. Эталон: routerSystemPrompt/RouterAction в MA.
26. **Режим «План» с подтверждением** (после 35): FSM останавливается после planning (awaitingPlan), пользователь одобряет/правит план кнопками. Эталон: approvePlan/replan в MA.
27. **LaunchAgent для пайплайнов** (после 36): выполнение cron/PR-watch при выключенном приложении.
28. **Sink'и пайплайнов кроме чата** (после 36): результат прогона в файл заметки vault, системное уведомление.

## Добавлено по итогам задачи 56 (2026-07-24)

29. **`/task-run` умеет сценарные профили** (после 56): скилл жёстко назначает `coder` стадией «Разработка», поэтому внутри конвейера `bugfix`/`refactor` не выбираются — сценарный профиль доступен только прямым вызовом техлида. Нужна развилка по форме задачи в `.claude/skills/task-run/SKILL.md`.

## Добавлено по итогам задачи 58 (2026-07-25)

Расхождения поведения, найденные при написании тестов (задокументированы тестом как факт, продовый код не менялся — вне объёма tests-only задачи 58). Каждое — потенциальный багфикс-профиль.

30. **`RagVector.cosine` возвращает NaN на Infinity-компоненте** (`RAG/RagVector.swift:30-34`): `guard na > 0` ложен для NaN, но проходит для `Infinity`, и до деления доходит `∞/∞ = NaN`. Комментарий обещает диапазон `[-1, 1]`. NaN-score способен исказить порядок `topK`. Решить, клампить ли Infinity или явно возвращать 0.
31. **`CodeRegionDetector`: незакрытый код-блок не защищает содержимое** (`Editor/CodeRegionDetector.swift:20-23`): ` ``` ` без закрывающей тройки → `excludedRanges` пуст, весь текст после неё открыт для inline-парсеров (wikilinks/чеклисты и т.п.). Смежно: ` ```code``` ` в середине строки случайно ловится инлайн-регексом. Детектор приблизительный — оценить, нужна ли защита «до конца документа» при незакрытой тройке.
32. **`FrontmatterParser`: пустой frontmatter-блок теряется при сериализации** (`Links/FrontmatterParser.swift:64`): `"---\n---\nтело"` парсится в `entries=[]`, а `serialized()` при пустых entries отдаёт только body, срезая маркеры `---\n---`. Нарушает заявленный в шапке модуля byte-exact round-trip (единственный найденный случай).

## Добавлено по итогам задачи 59 (2026-07-26)

33. **Пример `swift test --filter ToolPermissionsTests` в CLAUDE.md и Tests/SecondBrainTests/CLAUDE.md не матчит ни одного теста**: `--filter` фильтрует по имени класса `XCTestCase`, а не по имени файла — `ToolPermissionsTests.swift` содержит классы `ToolPermissionPolicyTests`/`ToolRiskClassifierTests`, класса `ToolPermissionsTests` не существует (проверено эмпирически: `swift test --filter ToolPermissionsTests` → «Executed 0 tests, with 0 failures»). Обнаружено при сборке `smoke/impact-map.tsv` (задача 59), где потребовались реальные имена классов. Поправить пример в обоих CLAUDE.md на класс, который правда существует.
34. **Смоук вкладок настроек через `secondbrain://settings/<tab>`** (после 59): раздел «Настройки» в сайдбаре — плейсхолдер, реальные вкладки живут в отдельном окне через `SettingsLink` (кнопка AX-невидима). Deep-link на вкладку работает (`open "secondbrain://settings/providers"` → `check "Провайдеры"` PASS), но `scripts/ui.sh cmd_open` умеет только `section/<slug>`. Мелко расширить `cmd_open` под форму `open Настройки/<Вкладка>` (карта подпись→slug из `SettingsTab`, маркер — подпись вкладки), затем углубить `smoke/settings.txt` до реальных вкладок вместо плейсхолдера. Обнаружено в задаче 59.
35. **Расширить блок-лист `DESTRUCTIVE` в `ui.sh` подписями создающих кнопок** (перед появлением `click` в смоук-сценариях): `scripts/ui.sh:33` блокирует Записать/Удалить/Push/Commit/Индексировать/…, но НЕ «Новая заметка/Новая папка/Новый чат/Новый пайплайн» — клик по ним создаёт данные в живом vault (инвариант №1). Пока сценарии `check`-only, это латентно; задача 60, добавив `click Новая заметка`, выстрелит. Добавить создающие подписи в regex (пред-существует с задачи 49). Обнаружено в ревью задачи 59.
36. **Deep-link не должен открывать новое окно** (`Sources/SecondBrain/App`, после 61): каждый `open "secondbrain://section/<slug>"` создаёт новое окно вместо навигации в существующем (SwiftUI-сцена на `onOpenURL`). Харнесс компенсирует (`close_extra_windows` в `ui.sh`), но для пользователя, кликающего ссылку, поведение тоже неверное. Обнаружено в задаче 61.

## Добавлено по итогам задачи 47/56 (2026-07-26)

37. **`ProjectToolsProvider.resetDocsIndex()` — вероятно мёртвый код** (`Tools/ProjectToolsProvider.swift:93-96`): после задачи 47 отдельная кнопка «Сброс» заменена на «Перестроить индекс», которая вызывает `docsIndex.reset` внутри `rebuild` напрямую. Проверить вызывающих `resetDocsIndex()` при следующем проходе по `Tools/ProjectToolsProvider.swift` и удалить, если подтвердится. Обнаружено `refactor`-профилем при смоуке задачи 56.

## Добавлено по итогам задачи 68 (2026-07-27)

38. **`CodeRegionDetector`: `\Z` вместо `\z` в regex незакрытого код-блока** (`Editor/CodeRegionDetector.swift:18`, после 68): `\Z` в ICU-regex матчит позицию перед финальным line-terminator, а не абсолютный конец ввода — для документа с trailing `\n` последний символ выпадает из защищённого региона. Практический вред минимален (`\n` сам по себе не парсится как разметка), но заявленное поведение «до конца документа» не совсем точно реализовано и не покрыто тестом на файл с trailing newline. Заменить на `\z` и добавить граничный тест. Обнаружено ревьюером в задаче 68.

## Добавлено по итогам задачи 70 (2026-07-27)

39. **Устаревшие комментарии после смены `WindowGroup` → `Window`** (после 70): `Sources/SecondBrain/App/AppModel.swift:5` упоминает «сцены (`WindowGroup` и `Settings`)» — теперь главная сцена `Window`, не `WindowGroup`. `scripts/ui.sh:41-42,73-76` описывают старое поведение («deep-link плодит окна») как текущую проблему, хотя задача 70 её устранила (харнесс-костыль `close_extra_windows` теперь избыточен, но не убран). Мелкая правка комментариев/харнеса при следующем проходе по этим файлам. Обнаружено ревьюером в задаче 70.

## Добавлено по итогам задачи 80 (2026-07-27)

40. **`GitHubTokenSection.body` синхронно читает Keychain и вешает главный поток** (`Settings/ToolsSettingsTab.swift:177`, антипаттерн 5 «I/O в View»): `KeyStore.hasKey(for:)` → `SecItemCopyMatching` вызывается прямо в `body` вкладки «Инструменты» на каждой перерисовке. На машине разработчика это стабильно вешает главный поток ещё до рендера формы — `./scripts/ui.sh preflight` после `open "secondbrain://settings/tools"` таймаутит намертво (диагностировано ревьюером задачи 80 через `sample` на процессе). Воспроизводится и до задачи 80 (коммит `d1bedc3`) — не новая регрессия. Нужно вынести чтение Keychain в `@State`/`.task` (асинхронно, вне `body`), как это уже сделано в других секциях настроек с внешним I/O.

41. **Смоук-сценарии под фичи задач 78/79** (после 78, 79): `smoke/notes.txt` не пополнен сценариями «переименование заметки с обновлением ссылок» и «новая заметка из шаблона» — правило «под новую фичу — смоук-сценарий» (`/smoke-cycle`) отработано не полностью; фичи покрыты юнит/интеграционными тестами и точечными AX-проверками в самих задачах. Дописать сценарии, когда AX-доступ к вкладкам/меню стабилизируется (см. п.40). Отмечено финальным ревью ветки execution-loop (2026-07-28).

## Добавлено по итогам задачи 81 (2026-07-31)

42. ~~**Вторая итерация раздела «Тюнинг»** — сборка датасета из vault внутри приложения (сейчас только `build_dataset.py`), экран сравнения baseline/тюн с автопроверками из `criteria.md`, история замеров с метриками, регистрация обученного адаптера в `FunctionRouter` как выбираемой модели, редактирование примеров датасета, облачный тюн.~~ → частично 82 (экран сравнения baseline/тюн, автопроверки, история замеров). Осталось в бэклоге: сборка датасета из vault внутри приложения, регистрация обученного адаптера в `FunctionRouter` как выбираемой модели, редактирование примеров датасета, облачный тюн.
43. **Пословный диф в примере датасета** (`FineTune/FineTuneDatasetViews.swift`, после 81): переиспользован `UnifiedDiff` — он построчный, и на парах «тема → длинный пост» честно вырождается в «заменено целиком». Для датасета диктовки (правка внутри строки) пословный диф был бы заметно информативнее. Нужен intra-line diff поверх существующего LCS.
44. **`BackgroundProcessRegistry` не умеет `unregister`** (`LocalRuntime/BackgroundProcessRegistry.swift:60`, после 81): массив растёт до `terminateAll()`, снятия с регистрации нет. Завершившиеся процессы остаются в нём мёртвыми ссылками. Практически безвредно (`runningCount` фильтрует по `isRunning`, а `FineTunePidProcess.detachFromQuit()` обходит проблему для своего случая), но при частых коротких прогонах список копится за сессию. Обнаружено при реализации задачи 81.
45. **PID reuse не отсекается по отпечатку процесса** (`FineTune/FineTuneRunner.swift`, после 81): живость прогона определяется по `kill(pid, 0)`, как и в `train.py`. За час прогона и перезапуск приложения система может переиспользовать pid — тогда чужой процесс покажется живым тюном, и «Остановить» ударит по нему. Конструктор `FineTunePidProcess` уже отсекает pid ≤ 1 (самый опасный случай), но сверки отпечатка (`ps -o lstart=,command=` при регистрации и перед сигналом) нет. Добавить, когда появится сценарий долгих прогонов через перезапуск. **Отдельно опаснее ручной кнопки:** совпадение pid со своей `.running`-записью прошлой сессии затягивает чужой процесс в `adoptIntoRegistry`, и тогда его гасит уже `terminateAll()` на выходе — без участия пользователя, вопреки инварианту №2.
46. **Смоук раздела «Тюнинг» не покрывает выбор строки в списке** (`smoke/finetune.txt`, после 81): AX-клик по строке `List(selection:)` не доводит выбор до стора — та же проблема воспроизводится в существующем «Пайплайны» на строке «Code Review», это системное ограничение синтетического клика в текущей версии SwiftUI/macOS, не регрессия задачи 81. Из-за этого экран «Датасет» с дифом «вход → эталон» и график прогона проверены чтением кода и данными, но не через AX. Пересмотреть, когда появится рабочий способ выбирать строку списка из харнесса.
47. **Хвосты раздела «Тюнинг» после ревью задачи 81** (после 81), каждый — правка на строки, не на архитектуру:
    - `FineTuneRunViews.swift` — `displayedRun = activeRun ?? selectedRun`: пока прогон бежит, выбор исторического прогона в средней колонке игнорируется, и «Взять лучший чекпоинт» уходит в workdir бегущего, а не выбранного.
    - Тестовые дыры: `tailedRunID` не ассертится ни одним тестом (дописывается строкой в существующие), `checkEnvironment()` не покрыт вовсе. Плюс нет интеграционного теста на то, что `AppModel.wire()` действительно ставит пробу выхода через `FineTuneQuitGuard.activeRun(in:)` — а именно в `wire()` был блокирующий дефект первого круга ревью.
    - Смена `projectRepoPath` пересканирует датасеты, но не зовёт `checkEnvironment()`: пока репозиторий не задан, экран «Прогон» диагностирует «поставь mlx-lm» вместо «не задан репозиторий», хотя список датасетов рядом показывает верную причину.
    - После успешного «Остановить обучение» из диалога выхода стор не обновляется — при следующем старте `normalize()` пометит прогон `interrupted`, а не `stopped` (косметика истории).
48. **Слепое сравнение baseline и тюна + автопроверки в приложении** (после 82): пары ответов без подписей и в перемешанном порядке, оценка 1–5 по четырём осям из `finetune/criteria.md`, счёт и вердикт по правилу «тюн выигрывает ≥7 из 10» — единственный пункт `criteria.md`, который на первом прогоне так и не выполнили (зафиксировано там же). Плюс перенос `style_checks.py`/`dictation_checks.py` в Swift с тестом паритета на закоммиченных артефактах (`evaluate.py` даёт 64/80 baseline и 70/80 тюн), генерация ответов тюна кнопкой (`baseline.py --adapter`, те же промпты и температура, что у baseline) и история замеров с экспортом строкой в `criteria.md`. Вынесено из 82, когда выяснилось, что для сдаваемого результата нужен читаемый набор артефактов, а не оценочный инструмент.
49. **`scripts/ui.sh`/`ax.applescript`: клик по строке `List(selection:)` не работает вовсе** (обнаружено на 82, раздел «Тюнинг», средняя колонка «Датасеты»): в отличие от сайдбара («срабатывает через раз»), клик по тексту внутри строки списка датасетов не сменил выбор ни разу за 6 попыток (`click`, затем `perform action "AXPress"` — оба доступных в `click el` пути). Тот же ряд, выбранный через System Events `select` (не через `ui.sh`), срабатывает мгновенно — значит, `NSTableView`/`AXOutline` в этой версии macOS слушает нативное `AXSelect`/`select`-действие, а не позиционный клик по вложенному `AXStaticText`. Добавить в `ax.applescript` режим `select <подпись>` (найти строку по подписи внутри, вызвать `select` на найденной `AXRow`) — без этого любой экран, где сценарию нужно кликнуть в конкретную строку списка (не только «Тюнинг»), обходится вручную, как здесь.
50. **`Toggle` вне `Form`/`List` не отдаёт AX-название** (обнаружено на 82, экраны «Датасет»/«Baseline»): `Toggle("Показать различия", isOn:)` в обычном `VStack` виден в Accessibility только как `AXCheckBox` со значением "0"/"1" — ни `name`, ни `value`, ни `description` не содержат текста метки, `check`/`click` по подписи не находят элемент (тот же класс проблемы, что `DisclosureGroup` и сегментный `Picker`). Если тумблер понадобится кликать/ассертить смоуком — обернуть в `Form`/`Section` (там подпись отображается) или заменить на `Button` с текстовым `Label`, как уже сделано для переключателя сегментов задачи 81.
51. **`FineTuneRunner.defaultRunCLI` не регистрирует процесс в `BackgroundProcessRegistry`** (`FineTune/FineTuneRunner.swift`, после 82): команды `train.py` (`start`/`stop`/`best`) запускают `Process` мимо реестра — пробел инварианта №2, оставшийся с задачи 81. Сам тюн от этого не страдает (обучение detached, его гасит `FineTunePidProcess`), но короткоживущий процесс CLI при выходе приложения не гасится. Новый `defaultRunValidate` регистрирует корректно — привести к нему. Обнаружено ревьюером в задаче 82.
52. **Мелочи раздела «Тюнинг» из ревью задачи 82** (после 82): логика построения дифа продублирована — инлайн в `FineTuneBaselineViews` против `static diffText` в `FineTuneDatasetViews`, включая одинаковую строку «(нет отличающихся строк)»; `FineTuneValidationParser` теряет `where` не-путёвых ошибок (`split`), пользователь видит текст без указания проверки; в `FineTuneViewModel.validate` сверка поколения стоит до снятия `isValidating` — сейчас недостижимо из-за входного guard, но если ветка сработает, кнопка «Проверить датасет» заблокируется навсегда; на повторном прогоне `smoke/finetune.txt` пара click/check по валидации ничего не доказывает, потому что результат персистится и отрисован ещё до клика.
53. **Мелочи раздела «Тюнинг» из ревью задачи 83** (после 83, 2026-08-01): `FineTuneRunner.defaultRunBaseline` ≈ 85% копия `defaultRunValidate` (pipe, вотчдог, чтение вывода) — вынести общий хелпер «запусти скрипт с таймаутом» (туда же смотрит пункт 51 про регистрацию `defaultRunCLI`); `FineTuneImportCore.invalidStructureReason` на строке с тремя валидными ролями, но отвергнутой `parseExample`, диагностирует «не JSON» — вводит в заблуждение, нужно отдельное сообщение; `FineTuneDatasetImporter.readSource` на любую ошибку чтения (нет файла, нет прав) отвечает одним текстом «проверьте кодировку UTF-8»; `ContinuationBox: @unchecked Sendable` в `FineTuneRunnerTests` — запись из актора, чтение из yield-цикла, формальная data race (работает, но хрупко).
54. **Проверка `assignee` в датасете встреч не переживает перевод** (после 84, 2026-08-01): `build_meetings_dataset.py` обнуляет `assignee`, не встречающийся в тексте окна, но делает это по английскому исходнику. После `translate_ru.py` роль в русской реплике часто стоит в косвенном падеже («для нашего промышленного дизайнера»), и дословного совпадения с эталоном нет — в закоммиченном `meetings/data/raw.jsonl` таких поручений 6 из 579. Семантически инвариант держится, но проверки после перевода нет вообще, и следующий прогон переводчика может дать настоящее нарушение молча. Место для неё — `actionitem_calibrate.py`: он и так обходит все эталоны. Заодно пересмотреть саму проверку: в английском исходнике она почти всегда удовлетворяется меткой говорящего («Industrial Designer: …»), то есть фактически спрашивает «эта роль говорила в окне», а не «названа исполнителем» — 0 срабатываний на 579 это подтверждают.
55. **Прогнать тюн датасета встреч и заполнить «Замеры»** (после 84, 2026-08-01): датасет, baseline (полный балл 1 из 10, проверок 45/65) и правило приёмки готовы, обучение вынесено из задачи 84 — LoRA 7B держит ~9.2 ГБ из 16 и на занятой машине роняет систему. Команда и порядок — в `finetune/meetings/README.md`; запускать на свободной машине, не собирая параллельно проект и не запуская .app. После прогона: `best --install`, снять `tuned/`, `evaluate.py --checks actionitem`, заполнить раздел «Замеры» в `finetune/meetings/criteria.md`.

## Добавлено по итогам задачи 85 (2026-08-01)

56. **Батч-прогон уверенности остаётся на захардкоженной 7B и легаси-адаптере** (после 85; чатная часть закрыта → 92): чат `.tuned` теперь поднимает пару (база прогона из `FineTuneStore`, его адаптер per-model), но `runBatch`/`ConfidenceBatchRunner` по-прежнему используют `MlxServerConfig.defaultModel` + `adapters/adapters.safetensors` — батч всегда тестирует легаси-7B-тюн и несовместим с мульти-модельными тюнами. Нужно провести выбор пары (база+адаптер) и в батч.
57. Осиротевший mlx_lm.server пережил завершение приложения (обнаружено 2026-08-01 при
    установке задачи 87 поверх работавшего приложения): порт 18765 остался занят, новый
    инстанс честно отказался адоптировать чужой процесс. Проверить путь
    `pkill SecondBrain` → `applicationWillTerminate` → `BackgroundProcessRegistry.
    terminateAll` для процесса `MlxServerManager`. Диагноз (2026-08-01, воспроизведено
    дважды): `pkill` шлёт SIGTERM, а NSApplication при SIGTERM НЕ вызывает
    `applicationWillTerminate` — чистка реестра работает только при штатном Cmd+Q.
    Варианты: sigterm-хендлер в AppDelegate (DispatchSource.makeSignalSource) с вызовом
    terminateAll, и/или адопция «своего» осиротевшего mlx-сервера по pid-файлу при
    старте (как FineTuneRunner.adopt). Пользовательский Cmd+Q не затронут.

## Добавлено по итогам задачи 93 (2026-08-01)

58. **Строитель `localTune` в `AppModel` не покрыт тестами; логика «run vs legacy»
    размножилась** (`App/AppModel.swift`, ревью 93 + follow-up): решающая часть
    (selectTunedRun → легаси-fallback → проверка `adapters.safetensors` → конфиг со
    вторым портом) — inline-замыкание; резолвер и VM тестируются на моках. После
    follow-up выбор источника адаптера повторяется в ТРЁХ местах: строитель в
    AppModel, `TuningChatViewModel.send()` (.tuned), `availableTunedModels`; плюс
    литерал `"adapters"` — третья точка знания легаси-пути. Вынести в чистую функцию
    `TuneSelection` «runs + baseModel → (adapterURL, model)?» (`fileExists` —
    инжектируемый параметр) + таблица случаев; в AppModel оставить только сборку
    провайдера.
59. **Пикер цели эскалации не исключает базу текущего чата** (`FineTuneChatViews`,
    ревью 93): выбор «тюн-7B → тот же тюн-7B» осмысленности не имеет, но поднимет
    второй сервер с той же моделью (2× память). Скрыть в списке базу, совпадающую с
    активной парой чата, или дать явный warning.

## Добавлено по итогам задачи 97 (2026-08-02)

60. **`selfCheckEmptyPrompt` уходит и на непарсибельный primary при выключенном
    constraint** (`Confidence/ConfidencePipeline.swift`, ревью 97): при constraint off
    и ответе-мусоре `expectedCount == 0`, и self-check спрашивает «ты ответил, что
    поручений нет» — неправда про собственный ответ модели. Достижимо только с
    выключенными «Проверками»; различать «пустой список» и «не распарсился» при выборе
    промпта.

## Добавлено по итогам задач 99–102 (prompt-инъекции, 2026-08-08)

61. **Детерминированные защиты от инъекций поверх промпта** (после 99) → 100 (частично: а, в): задача 99 закрыла текстовый слой, но три вещи промптом не закрываются и ждут кода — (а) egress: запрет внешних доменов в выводе модели и allowlist, иначе markdown-эксфильтрация держится только на правиле 6; (б) анализ траектории диалога против многоходовой раскрутки (crescendo/echo chamber) — правило действует на текущий ход, а атака живёт в последовательности; (в) `sanitizeUntrusted` применяется только к `[RAG_CONTEXT]` и `[PROJECT_DOCS]`, но не к результатам инструментов и MCP-ответам — тот же невидимый Unicode доедет до модели через `ToolUseLoop`. Пункты (а) и (в) уходят в задачу 100; (б) crescendo остаётся здесь. Набор проверок — в заметке vault «Проверка безопасности агента — 15 prompt-инъекций».
62. **Тумблер «Защита» (задача 101) не гасит anti-injection оговорку в спецслучае {vault} через `RagIndexManager.retrieveForChat`** (после 101): `KnowledgeBaseManager.retrieve(secure:)` прокидывает флаг в `KnowledgeMerge.merge` (мультибаза), но для ровно одной включённой базы {vault} делегирует старому пайплайну `RagIndexManager.retrieveForChat` → `RagRetriever.retrieve` → `buildBlock`, который параметра `secure` не получает. Практический эффект узкий: путь используется только когда `ragAsTool` выключен вручную (дефолт — `true`, тогда RAG идёт через `rag_search`, уже покрытый тумблером). Прокинуть `secure` через `retrieveForChat` в `RagRetriever.retrieve` до `buildBlock`.
63. **`fetch_url`: кап и SSRF** (после 102): (а) лимит 64 КБ применяется после полной загрузки тела в память — настоящий лимит требует стримингового чтения; (б) GET на любой URL не блокирует внутренние адреса (`127.0.0.1`, `169.254.169.254`) и редиректы туда — SSRF; сейчас осознанно `.write` + подтверждение. Оценить egress-allowlist (пересекается с #61 а).
