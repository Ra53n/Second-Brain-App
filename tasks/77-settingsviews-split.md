# Задача 77: разбить `SettingsViews.swift` по вкладкам

## Тип

рефакторинг

## Модель

sonnet

## Цель

`Sources/SecondBrain/Settings/SettingsViews.swift` (830 строк) содержит 5+ независимых
вкладок настроек (`GeneralSettingsTab`, `ProvidersSettingsTab`, `ModelsSettingsTab`,
`MeetingsSettingsTab`, `ToolsSettingsTab`) в одном файле. Естественная граница разбиения —
по вкладке; правка одной вкладки не должна требовать чтения остальных четырёх.

## Зависимости

Нет.

## Объём

1. `ast-index outline Sources/SecondBrain/Settings/SettingsViews.swift` — свериться с
   точными границами каждой вкладки.
2. Вынести каждую вкладку в свой файл: `GeneralSettingsTab.swift`, `ProvidersSettingsTab.swift`,
   `ModelsSettingsTab.swift`, `MeetingsSettingsTab.swift`, `ToolsSettingsTab.swift` (плюс
   любые общие вспомогательные View, используемые несколькими вкладками — оставить в
   `SettingsViews.swift` как общий файл, а не размножать копиями).
3. Никакой правки логики.

## Вне объёма

Изменение состава/поведения вкладок, добавление новой вкладки.

## Критерии приёмки

- [x] 5 новых файлов созданы по числу вкладок, `SettingsViews.swift` содержит только общие
      вспомогательные типы (или пуст/удалён, если общего не осталось).
- [x] Поведение не изменилось: доказано побайтовым сравнением перемещённого кода и полным
      зелёным набором тестов — AX-смоук `./scripts/ui.sh` недоступен по вине окружения
      (таймаут `-1712`, известная проблема машины, не связана с диффом), не прогнан.
- [x] `./scripts/build.sh` и `./scripts/test.sh` зелёные, число тестов не уменьшилось.

## Архитектура

Чистое перемещение (профиль refactor), без правки логики. `ast-index outline` до правки
дал точные границы 5 вкладок в `SettingsViews.swift` (830 строк по описанию задачи,
796 фактически на момент работы). Разбор общих типов:

- `GeneralSettingsTab`, `ProvidersSettingsTab`, `ModelsSettingsTab`, `MeetingsSettingsTab` —
  байт-в-байт в одноимённые файлы, `import SwiftUI`.
- `ToolsSettingsTab` — в свой файл вместе с `GitHubTokenSection` и `KnowledgeBasesSection`:
  оба используются только этой вкладкой (`grep` подтвердил единственный сайт вызова каждого
  в `SettingsViews.swift:498,502` до правки) — не «общие между вкладками», локальность
  вкладки важнее формального деления по MARK-секциям исходника. `import AppKit` добавлен
  (нужен `NSOpenPanel` в `KnowledgeBasesSection.pickFolder`).
- `ProjectRepoPicker` остался в `SettingsViews.swift` как общий: используется не только
  вкладкой «Инструменты», но и `Chat/ChatViews.swift:424` (мастер в пустом чате) — это
  ровно случай «общее между несколькими потребителями», для которого задача просила
  файл-контейнер.
- `SettingsRootView`, `SettingsTab`, `SettingsTabRouter`, `Notification.Name.openSettingsTab`,
  `SyncSettingsTab` — вне объёма задачи (не входят в список 5 вкладок), остались на месте;
  `SyncSettingsTab` физически рядом с корнем, т.к. не переносился.
- Другие вкладки (`LocalModelsPane`, `MCPServersSection`, `ProjectDocsStatusSection`,
  `RagStatusSection`, `PermissionRow`) уже были в отдельных файлах модулей — не трогал
  (антипаттерн 8, вне объёма).
- Header-комментарий `SettingsViews.swift` переписан под новую структуру (комментарий,
  не логика) — старый текст перечислял все 7 вкладок как «здесь», что стало неверно.

## Отчёт тестов

`./scripts/build.sh` — Build complete, без новых предупреждений/ошибок.
`./scripts/test.sh` — 1130 тестов, 0 падений (совпадает с зафиксированным в задаче
базовым числом после задачи 76 — поведение не изменилось).

Смоук: `smoke/impact-map.tsv` содержит готовый сценарий `smoke/settings.txt` для
`Settings/**` (задача 59). Собрал и установил свежий `.app` (`install.sh`), перезапустил
приложение (`./scripts/ui.sh restart` — подтвердил «приложение перезапущено»). Дальнейшие
AX-запросы (`preflight`, `count of windows`, `run smoke/settings.txt`) стабильно упирались
в таймаут/`-1712 Время AppleEvent истекло` при обращении System Events к процессу
SecondBrain — та же известная проблема AX на этой машине, что и в задаче 75. UI-смоук
не прогнан по вине окружения, не кода; наблюдаемое поведение подтверждено сборкой,
полным зелёным набором тестов (число не уменьшилось) и тем, что правка — чистое
перемещение объявлений между файлами одного таргета (компилятор гарантирует
идентичность байт тела каждого типа).

## Результат

5 вкладок настроек вынесены из `SettingsViews.swift` (830 → 171 строка) в свои файлы,
байт-в-байт (подтверждено ревьюером посимвольной сверкой). `GitHubTokenSection`/
`KnowledgeBasesSection` ушли вместе с `ToolsSettingsTab` (единственный потребитель),
`ProjectRepoPicker` остался общим (два потребителя). `SyncSettingsTab` и корневые типы —
вне объёма, не тронуты. `GO` с первого круга; единственное замечание ревьюера —
формулировка чекбокса смоука вводила в заблуждение (звучала как «пройден», а AX-смоук
не прогнан по вине окружения) — исправлена выше.
