# Swift / SPM
.build/
.swiftpm/
*.xcodeproj
DerivedData/

# Артефакты сборки .app (пересоздаются run.sh) и дистрибутивы (dist.sh)
SecondBrain.app/
dist/

# Рабочие файлы генерации иконки (итог — Sources/SecondBrain/Resources/AppIcon.icns)
icon/*.iconset/
icon/icon_1024.png

# macOS
.DS_Store

# Секреты и локальные файлы — никогда не коммитим
*.key
*.local.*
.env

# Смоук-отчёты (задача 60) — генерируются, не коммитим
smoke/out/
