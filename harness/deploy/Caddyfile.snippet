# Ручная вставка маршрута, если deploy.sh не смог пропатчить Caddyfile.
# Добавить ВНУТРЬ существующего блока домена, рядом с handle /gw/*.

    handle /harness/* {
        reverse_proxy 127.0.0.1:3500
    }
